#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(){
    srand(time(0));
    int x[100] = {0};
    for (int i = 0; i<5; i++){
        x[i] = rand()%100;
        printf("%d\n",x[i]);
    }
}
